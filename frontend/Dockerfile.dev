FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# No need to run npm install here - docker-compose will do it
# This keeps the image lightweight

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"]
